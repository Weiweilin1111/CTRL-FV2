# CtrlF v3 核心依賴（已移除 chromadb / sentence-transformers / Django 等冗餘）
# torch / torchvision 請依硬體另行安裝（CUDA 版本對應）
ultralytics>=8.4
opencv-python>=4.10
numpy>=1.26
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
PyYAML>=6.0
streamlit>=1.45
requests>=2.32
pillow>=10.0
# CLIP（OpenAI 版）由 ultralytics 在使用 YOLO-World 時自動安裝:
#   pip install git+https://github.com/ultralytics/CLIP.git
